<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <property name="LOG_PATTERN"
              value="%d{HH:mm:ss.SSS} %-5level [%X{traceId:-}] %logger{36} - %msg%n"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- Dev-only mirror to a fixed file so logs are tailable no matter how the
         app is launched (IntelliJ Run, gradle bootRun, …). Overwritten each
         start (append=false). Override the path with -DDEV_LOG_FILE=… ; set it
         empty to send the file to /dev/null in prod. -->
    <property name="DEV_LOG_FILE" value="${DEV_LOG_FILE:-/tmp/erp-backend.log}"/>
    <appender name="DEVFILE" class="ch.qos.logback.core.FileAppender">
        <file>${DEV_LOG_FILE}</file>
        <append>false</append>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="DEVFILE"/>
    </root>

    <logger name="com.company.erp" level="DEBUG"/>
    <logger name="org.hibernate.SQL" level="WARN"/>
    <logger name="org.springframework.security" level="INFO"/>

</configuration>
