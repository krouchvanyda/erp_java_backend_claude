; Populate $_ENV from the process environment (adds "E" to the order). Without
; this, `php artisan serve` cannot propagate container env vars (e.g. DB_HOST)
; to its built-in-server child process, and Laravel falls back to .env values.
variables_order = "EGPCS"
